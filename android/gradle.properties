org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter's Gradle plugin still reads the legacy Android extension on AGP 9.
# Keep these compatibility flags until the Flutter toolchain can use the new
# DSL; removing them prevents any Android build from configuring.
android.newDsl=false
android.builtInKotlin=false
